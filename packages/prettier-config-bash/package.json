{
  "name": "prettier-config-bash",
  "version": "0.0.0",
  "private": true,
  "license": "MIT",
  "scripts": {
    "clean": "rm -rf .turbo && rm -rf lint-results && rm -rf .nyc_output && rm -rf node_modules"
  },
  "dependencies": {
    "import-sort-style-module": "^6.0.0",
    "import-sort-style-module-alias": "^1.1.0",
    "prettier": "^2.8.8",
    "prettier-plugin-import-sort": "^0.0.7"
  },
  "publishConfig": {
    "access": "public"
  }
}